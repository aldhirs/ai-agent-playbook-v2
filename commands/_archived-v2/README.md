# Archived v2.x Commands

These commands are from playbook v2.x workflow (7-gate model). Superseded by v3.0 `/spec` command (single command, autonomous AI through Phase 1).

| v2.x command | v3.0 replacement |
|---|---|
| `/groom <epic>` | `/spec <epic>` (autonomous Phase 1: invokes pm + solution-architect + ui-impact-analyst) |
| `/tech-design <slug>` | (folded into `/spec`, output di SPEC-BE.md § 3-9) |
| `/plan <slug>` | (folded into `/spec`, output di SPEC-BE/FE per-section breakdown) |

**Kalau masih pakai workflow v2.x**, commands di sini valid. Untuk feature baru, rekomendasi pakai `/spec`.

Migration guide: lihat `../../MIGRATION-v2-to-v3.md`.
